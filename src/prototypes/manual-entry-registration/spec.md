# 燃煤入厂登记

> 人工办理点登记界面，面向入厂管理员。支持切换入厂点、按菜单权限加载二级功能（来煤/转场煤/非煤物资/出厂登记），配置驱动表单字段与操作按钮。

## 📋 功能说明

### 1.1 核心目标

入厂管理员在**一屏固定布局**内完成燃煤入厂登记：登记表单与入厂车辆列表同时可见，页面本身不纵向滚动（仅列表内部滚动）。

1. **切换入厂点**：顶部下拉单选入厂点，浏览器 `localStorage` 缓存上次选择，再次进入默认加载缓存入厂点。
2. **二级功能导航**：来煤登记、非煤物资登记、转场煤登记、车辆办卡、出厂登记，按登录人菜单权限动态加载。**仅来煤登记有完整界面**，其余菜单点击后展示「暂未开发」。
3. **布局优化**：顶部上下文栏（入厂点 + 功能 Segment 导航）与表单操作区分层；数据获取按钮分组展示。
4. **来煤信息获取**：人工选择计划、计划码扫码、云驿扫码、煤样卡查询、车牌联想等方式回填表单。
5. **配置驱动表单与操作**：字段 `FIELD_*`、操作 `OPERATION_*` 按入厂点配置动态加载。
6. **入厂车辆列表**：仅在来煤登记模块展示。

### 1.2 使用角色

| 角色 | 主要任务 |
|------|---------|
| 入厂管理员 | 人工录入送货单、读卡、扫码，办理入厂手续与发卡 |
| 门岗班长 | 切换入厂点、核对登记信息、查看入厂车辆列表 |
| 燃料集控 | 抽查登记记录 |

## 📊 业务规则/约束

### 2.1 业务规则

#### A. 入厂点与模块

1. 顶部 **入厂点下拉** 切换 MEOR 模块类型对应入厂点；选择后写入 `mer_last_site_id` 缓存。
2. **二级功能导航** 根据 `MODULE_MENUS.permitted` 过滤展示；**仅「来煤登记」渲染完整界面**，其余模块展示 Empty「暂未开发」，不展示数据获取按钮与入厂车辆列表。
3. 切换入厂点或功能模块时清空表单。

#### B. 表单字段（配置项）

| 表单字段 | 配置项 | 约束 |
|---------|--------|------|
| *车牌号码 | FIELD_vehicleNo | 可编辑；单独一行置于分区标题上方；输入 ≥3 位联想车牌；不合规提示「车牌错误」 |
| 车辆卡号 | FIELD_cardNo | 只读；与车牌号码同排独立一行 |
| 供应商/矿点/煤种/品名/运输单位 | — | 非配置字段，默认只读加载 |
| 矿发毛重 t | FIELD_mineHairGrossWeight | 配置可编辑时为小数步进器，两位小数 |
| 矿发皮重 t | FIELD_mineHairTare | 同上 |
| *矿发净重 t | FIELD_ticketHeight | 同上，登记必填 |
| 发站 | — | 只读 |
| 发站时间 | FIELD_fromDate | 按配置只读展示 |
| 采样位 | FIELD_simplingName | 可编辑时为汽车采样机下拉 |
| 过衡位 | FIELD_poundName | 可编辑时为汽车衡下拉 |
| 卸煤区域 | — | 只读 |
| 采样方式 | FIELD_simplingSource | 机械采样 / 人工采样，提交明文字符串 |
| 入厂卡/煤样卡 | FIELD_cardNo1 | 只读 |

#### C. 操作（配置项）

| 操作 | 配置项 | 约束 |
|------|--------|------|
| 人工选择计划 | OPERATION_manualSelectPlan | 右侧抽屉；当日来煤计划单选；可按供应商/矿点/煤种筛选；点击行选中；回填供应商、矿点、煤种、品名、运输单位、发站、发站时间、采样位、过衡位、预卸煤区 |
| 云驿扫码 | OPERATION_yunYiCode | 扫码弹窗；Esc/关闭退出；校验 15 分钟时效；按计划 ID 查计划；回填表单；提交含运单号 |
| 云驿自动模式 | OPERATION_yunYiCodeAuto | 二次确认后打开右侧抽屉持续读码；登记类型由 `GOABLE_AotoYunYiRegister`（enter/preEnter）决定；校验时效与计划后自动登记；运单结果保留不重置，动画回到等待下一车 |
| 计划码扫码 | OPERATION_planCode | 扫码弹窗；按计划 ID 查计划并回填 |
| 煤样卡查询 | OPERATION_cardNo1Search | 右侧抽屉读卡；5s 未读卡提示；**仅展示查询结果，不回填表单**；用户查看后关闭；可重新读卡 |
| 确认登记 | OPERATION_enterComfire | 校验必填 → 提交 → 成功清空表单并刷新列表 |
| 确认预登记 | OPERATION_preEnterComfire | 同上，成功提示「预登记成功」 |
| 确认登记并发卡 | OPERATION_cardNo1WriteAndenterComfire | 登记成功后写卡提示与结果 |
| 清除重置 | — | 常驻，清空表单 |
| 补发煤样卡（列表） | OPERATION_cardNo1Write | 已出厂不显示；写卡 5s 超时提示失败 |

#### D. 入厂车辆列表（仅来煤登记）

- 非来煤登记模块不展示列表区域。
- 不支持查询；按入厂时间逆序。
- 无任何列表操作配置时隐藏操作列；有配置时操作列 `fixed: 'right'`。
- 演示数据：`鲁B12876` 为已出厂，不显示补发煤样卡。

### 2.2 操作说明

- **数据类型**：登记单据 + 列表
- **数据源**：用户提供业务规约；前端 Mock（计划、预入厂、读卡、扫码示例）
- **关键操作**：
  - `入厂点切换`：下拉 + localStorage 缓存
  - `人工选择计划` / `计划码扫码` / `云驿扫码` / `煤样卡查询`：回填表单
  - `确认登记` / `确认预登记` / `确认登记并发卡`：提交登记
  - `清除重置`：清空表单
  - `补发煤样卡`：列表行操作

### 2.3 界面默认情况

- 进入页面按缓存加载入厂点；默认展示来煤登记。
- 点击非来煤登记菜单：表单区展示「暂未开发」，无列表与操作按钮。

## 🧭 布局与结构

- **布局模式**：一屏固定（根容器 `height: 100%` + `overflow: hidden`）。
- **顶部上下文栏**：白底卡片，左侧入厂点下拉，中间 Segment 式功能导航；来煤登记且有权限时右侧「云驿自动模式」按钮（二次确认后开抽屉）。
- **表单卡片**：来煤登记时，车牌号码与车辆卡号单独一行置于「来煤信息/矿发信息/入厂信息」标题上方；下方三列字段 + 底部登记按钮。
- **入厂车辆列表卡片**：仅来煤登记时展示。

## 🎨 视觉规范

### 设计规范来源

- **主题**：项目默认 `src/themes/antd-new`
- **设计规范**：`src/themes/antd-new/DESIGN-SPEC.md`
- **设计方法**：管理后台 `/skills/default-design-guide-minimal/SKILL.md`

### 组件状态

| 状态 | 表现 |
|------|------|
| 默认 | 白卡片 + 下划线只读行；可编辑字段 InputNumber/Select |
| 加载 | 提交全屏 Spin；写卡 message.loading |
| 空 | 字段「—」 |
| 异常 | 车牌错误、二维码失效、写卡失败 message |

## 🗂 文件结构

```
src/prototypes/manual-entry-registration/
├── index.tsx
├── spec.md
├── style.css
├── data.ts
├── parse.ts
└── components/
    ├── CardSearchDrawer.tsx
    ├── FormFieldRow.tsx
    ├── ModuleNav.tsx
    ├── PlanSelectDrawer.tsx
    ├── PlanScanModal.tsx
    ├── PlateInput.tsx
    ├── YunyiScanModal.tsx
    └── YunyiAutoDrawer.tsx
```

## ✅ 验收记录

- 验收脚本：待执行
- 浏览器验证项：
  - 入厂点下拉切换并缓存
  - 非来煤登记菜单展示「暂未开发」
  - 配置驱动字段与操作按钮
  - 人工选择计划抽屉含筛选
  - 云驿/计划码扫码 Esc 退出
  - 煤样卡读卡动画与超时
  - 三种登记提交与列表补发卡
  - 已出厂车辆无补发操作
